//next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // The i18n block has been removed.
  images: {
    // You can keep other configurations like this one.
  },
};

export default nextConfig;